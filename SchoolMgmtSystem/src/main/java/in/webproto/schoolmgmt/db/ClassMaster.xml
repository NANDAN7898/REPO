<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="ClassMaster">	

    <select id="getAll" resultType="in.webproto.schoolmgmt.to.ClassMasterTO">
  		SELECT id, className, sections, maxStudent FROM ClassMaster
    </select>
    
    <insert id="insert" parameterType="in.webproto.schoolmgmt.to.ClassMasterTO" useGeneratedKeys="true" keyProperty="id">
    	INSERT INTO ClassMaster (className, sections, maxStudent) VALUES (#{className}, #{sections}, #{maxStudent})
    </insert>
    
    <select id="getById" resultType="in.webproto.schoolmgmt.to.ClassMasterTO" parameterType="int">
    	SELECT id, className, sections, maxStudent FROM ClassMaster WHERE id = #{id}
    </select>
    
    <select id="getSectionByClass" resultType="in.webproto.schoolmgmt.to.ClassMasterTO" parameterType="int">
    	SELECT sections FROM ClassMaster WHERE id = #{id}
    </select>
    
    <update id="update" parameterType="in.webproto.schoolmgmt.to.ClassMasterTO">
    	UPDATE ClassMaster SET className = #{className}, sections = #{sections}, maxStudent = #{maxStudent} WHERE id = #{id}
    </update>
    
    <delete id="deleteById" parameterType="int">
    	DELETE FROM ClassMaster WHERE id = #{id}
    </delete>
    
</mapper>