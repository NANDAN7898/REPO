<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="UserDetailMaster">	

    <select id="getAuthenticationDetails" resultType="in.webproto.schoolmgmt.vo.AuthenticationVO" parameterType="String">
  		SELECT id as userId, username, password, enabled, firstName, lastName, role as authority 
  		FROM UserDetailMaster WHERE username = #{username}
    </select>
    
</mapper>