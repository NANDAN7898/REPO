<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="TeacherMaster">	

    <select id="getAll" resultType="in.webproto.schoolmgmt.to.TeacherMasterTO">
  		SELECT id, name, fatherName, dob, mobile, address, pin, state, district, email, joiningDate, highQualification, specialization, experience, subject, salary
  		FROM TeacherMaster
    </select>
    
    <insert id="insert" parameterType="in.webproto.schoolmgmt.to.TeacherMasterTO" useGeneratedKeys="true" keyProperty="id">
    	INSERT INTO TeacherMaster (name, fatherName, dob, mobile, address, state, district, email, joiningDate, highQualification, salary, specialization, experience, pin, subject) 
    	VALUES (#{name}, #{fatherName}, #{dob}, #{mobile}, #{address}, #{state}, #{district}, #{email}, #{joiningDate}, #{highQualification}, #{salary}, #{specialization}
    	, #{experience}, #{pin}, #{subject})
    </insert>
    
    <delete id="deleteById" parameterType="int">
    	DELETE FROM TeacherMaster WHERE id = #{id}
    </delete>
    
</mapper>